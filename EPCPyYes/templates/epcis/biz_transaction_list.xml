<bizTransactionList>
    {% for bt in business_transaction_list %}
        <bizTransaction {% if bt.type %}type="{{ bt.type.value or bt.type }}"{% endif %}>
            {{ bt.biz_transaction }}
        </bizTransaction>
    {% endfor %}
</bizTransactionList>
